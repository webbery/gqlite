#pragma once
#include <string>
#include <stack>

class GParserEngine {
// public:
//   GParserEngine();
//   ~GParserEngine();
//   int parse(const std::string& gql, AST** ast, char** error);

//   void pushAction(const grammar::GActionInfo& action);
//   bool popAction(grammar::GActionInfo&);
  
// private:
//   std::stack<grammar::GActionInfo> _actions;
};

