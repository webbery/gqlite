#include "Type/QueryCondition.h"
#include "Error.h"
#include "base/ast.h"

int GQueryConditionClause::Parse(struct gast* ast)
{
  bool isBin = false;
  //_preds = GET_ARRAY_VALUE(ast, isBin);
  return ECode_Success;
}
